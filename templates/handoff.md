{{/*
Frontmatter contract (TASK-00049): every value whose provenance is
user-supplied text MUST go through yamlq. A raw value containing ": "
renders invalid YAML (plain scalars cannot hold ": ") and breaks every
strict reader (VS Code Markdown preview, adb validate). Machine-generated
keys (task, template_version, generated_at) stay raw on purpose: the drift
regex in ticketdrift.go reads them unquoted, and yamlq would emit them
unchanged anyway. Do not remove yamlq when editing this template.
*/}}{{if .TemplateVersion}}---
template: ticket/handoff
template_version: {{.TemplateVersion}}
task: {{.TaskID}}
title: {{yamlq .Title}}
generated_at: {{.CompletedAt}}
---

{{end}}

# Handoff: {{.Title}}

**Task ID:** {{.TaskID}}
**Completed:** {{.CompletedAt}}

> **This file is for:** the close-out snapshot — what shipped, key trade-offs,
> what remains. **Not for:** the running progress log (`notes.md`) or the
> problem statement (`context.md`). **Write protocol:** generated by
> `adb task archive`; do not hand-edit — the ticket files are the handoff while
> work is live.

## Summary

{{.Summary}}

## What Was Done

{{range .CompletedItems}}
- {{.}}
{{else}}
- No items completed
{{end}}

## Key Decisions

{{range .Decisions}}
### {{.Title}}

{{.Description}}

**Rationale:** {{.Rationale}}

{{end}}

## Open Items

{{range .OpenItems}}
- [ ] {{.}}
{{else}}
- No open items
{{end}}

## Next Steps

{{.NextSteps}}

## References

{{.References}}
