version: "1.0"
defaults:
  ai: {{.AI}}
  priority: P2
  owner: ""
task_id:
  prefix: "{{.Prefix}}"
  counter: 0
  pad_width: 5
branch:
  pattern: "{type}/{repo}/{description}"
screenshot:
  hotkey: "ctrl+shift+s"
offline_mode: false
